server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: http://
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: secret